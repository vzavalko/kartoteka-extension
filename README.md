# Картотека — расширение Chrome

Сдаёт открытые вкладки в [Картотеку](https://vzavalko.github.io/kartoteka/):
забирает **настоящие заголовки** вкладок, закрывает их и освобождает память Chrome.

С версии 1.1 расширение разговаривает со страницей напрямую: вкладки приезжают
в открытую Картотеку без `⌘V`, а сама Картотека спрашивает у расширения название
для любого адреса, который вы в неё вставили.

Приложение, в которое всё складывается: [vzavalko/kartoteka](https://github.com/vzavalko/kartoteka)
· сайт https://vzavalko.github.io/kartoteka/

Короткая инструкция для того, кому вы это отправляете, — в [INSTALL.md](INSTALL.md).

## Установка

1. `chrome://extensions` → включить **Режим разработчика** (справа сверху).
2. **Загрузить распакованное расширение** → выбрать эту папку.
3. Значок появится на панели; закрепите его булавкой.

## Как пользоваться

**Разом.** Нажать значок (или `⌘⇧K`) → откроется список вкладок текущего окна.
Снять лишние галочки → **«Сдать N ссылок»**. Вкладки закроются, ссылки лягут в буфер,
откроется Картотека — остаётся нажать `⌘V`.

**Адрес без открытой вкладки.** Скопировали ссылку где-то на стороне — откройте попап:
адрес уже подставлен из буфера, нажмите **«Узнать»**. Расширение сходит на страницу,
достанет её настоящий заголовок и положит в буфер `Заголовок | адрес`. Дальше `⌘V`.

Работает и для нескольких адресов сразу (через пробел или с новой строки). Для YouTube,
Vimeo, SoundCloud, Flickr и TED название берётся из oEmbed, для остальных — из `<title>`
или `og:title` страницы. Кодировка определяется по заголовку ответа и `<meta charset>`,
так что `windows-1251` не превращается в кракозябры.

Страницы за логином отдадут заголовок страницы входа: запрос идёт **без куки**.
Для них надёжнее открыть вкладку и сдать её через `⌘⇧S` — там берётся заголовок
самой вкладки.

**По одной.** `⌘⇧S` на любой вкладке — она уходит в очередь и закрывается.
Счётчик на значке показывает, сколько накопилось. Когда надоест — открыть попап
и сдать всё одной кнопкой.

**Новая вкладка.** По умолчанию `⌘T` сразу открывает Картотеку вместо пустой страницы
Chrome. Встроить её во фрейм нельзя — claude.ai отдаёт `x-frame-options: SAMEORIGIN`, —
поэтому это честный переход на страницу, а не локальная копия.

Побочный эффект: после перехода фокус уходит из адресной строки, и начать печатать
адрес сразу не выйдет. Привычка — `⌘L`: она возвращает курсор в адресную строку
в любой момент. Если мешает, выключите в настройках расширения (значок → шестерёнка →
«новая вкладка открывает Картотеку») — тогда новая вкладка покажет локальную страницу
с кнопками, без обращения к сети.

Закреплённые вкладки не закрываются никогда. Служебные страницы (`chrome://`,
расширения, локальные файлы) в список не попадают: вне Chrome их адрес бесполезен.

## Как оно связано с Картотекой

Два пути, и работают они одновременно.

**Напрямую.** На `vzavalko.github.io/kartoteka/` расширение подключает к странице
скрипт-мост. Через него сданные вкладки попадают в Картотеку сразу, а страница
может спросить название для вставленного адреса. Проверить, что связь есть:
в Картотеке меню `⋯` → «Как складывать вкладки», сверху будет «Расширение подключено».

**Через буфер.** Если страница не ответила (другой адрес, мост ещё не поднялся),
ссылки всё равно лежат в буфере строками `Заголовок | адрес` — Картотека разбирает
такой формат при вставке. Этот путь не зависит ни от домена, ни от разрешений,
поэтому остаётся запасным навсегда.

## Разрешения

- `tabs` — прочитать заголовки и адреса вкладок, закрыть сданные.
- `storage` — очередь, адрес Картотеки и настройки.
- `clipboardRead` — подставить адрес из буфера в поле «узнать название».
  Буфер читается только при открытии попапа и никуда не отправляется.
- Доступ к сайтам (`<all_urls>`) — **необязательный**, включается галочкой в настройках
  или при первом нажатии «Узнать». Без него всё остальное работает; расширение просто
  не сможет сходить за заголовком — ни по кнопке, ни по просьбе страницы.
- Скрипт-мост подключается **только** на `vzavalko.github.io/kartoteka/*` и больше
  никуда.

Запрос к сайту делается ровно один — за заголовком, без куки, и качается только начало
страницы (до `</title>`, не больше 96 КБ). Никакой аналитики и никаких отправок наружу.

## Иконки

`icons/make_icons.py` рисует их без сторонних библиотек (PIL на машине нет).
Перерисовать: `python3 icons/make_icons.py` из папки `icons`.
